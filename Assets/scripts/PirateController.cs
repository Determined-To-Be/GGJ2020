using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.AI;

public class PirateController : MonoBehaviour
{ 
    //public float speed;
    //public NavMeshAgent nav;
    //public Transform playerPos;
    //public bool aggro = true;
    //public Transform[] waypoints;
    //public int numberOfWaypoints = 0;

    //private void Start()
    //{
    //    nav.speed = speed;
    //    // setting speed of enemy
    //    GotoNextPoint();
    //}
    //void GotoNextPoint()
    //{
    //    if (waypoints.Length == 0)
    //        return;
    //    nav.destination = waypoints[numberOfWaypoints].position;
    //    numberOfWaypoints = (numberOfWaypoints + 1) % waypoints.Length;
    //}

    //// Update is called once per frame
    //void Update()
    //{
    //    if (!nav.pathPending && nav.remainingDistance < 0.5f)
    //    {
    //        GotoNextPoint();
    //    }
    //    else
    //    {
    //        nav.destination = playerPos.position;
    //        // if enemy knows where player already is
    //        // enemy will go to player's position
    //    }

    //}
    
}
